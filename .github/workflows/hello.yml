name: Say hello

on: 
  push: 
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the menssage 
        run: echo "Hola Alexander"
      - name: Dime la fecha
        run: date        
